'use client';

import styles from './MottoFooter.module.scss';

export function MottoFooter() {
  return (
    <div className={styles.mottoContainer}>
      <h2 className={styles.mottoText}>First see it, then make it!</h2>
    </div>
  );
}
